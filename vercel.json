{
  "version": 2,
  "buildCommand": "pip install -r requirements-docs.txt && mkdocs build --site-dir site",
  "outputDirectory": "site",
  "installCommand": "pip install -r requirements-docs.txt",
  "framework": null
}
